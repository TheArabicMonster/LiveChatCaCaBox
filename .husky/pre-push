#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Temporarily disabled due to npm registry issues
# pnpm audit --audit-level=critical --only=prod
echo "Pre-push hook: skipping audit due to registry connectivity issues"
